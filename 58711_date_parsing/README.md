# 58711_date_parsing

## Build and install

```shell
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```shell
./dist/bin/dateparse
```

Should output something like:

```text
```

