# 70402_atexit

## Build and install

```console
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```console
$ ./dist/bin/demo --help
Usage: demo
   Demonstrate usage of atexit function.

$ ./dist/bin/demo
That's all, folks!
```

