# 58506_hexviewer

## Build and install

```shell
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```shell
./dist/bin/hexview
```

Should output something like:

```text
```

