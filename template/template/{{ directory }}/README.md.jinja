# {{ directory }}

## Build and install

```shell
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```shell
./dist/bin/{{ executable }}
```

Should output something like:

```text
```

