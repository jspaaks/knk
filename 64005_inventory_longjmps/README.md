# 64005_inventory_longjmps

Or, a lesson in unwholly control flow.

## Build and install

```console
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```console
./dist/bin/inventory
```
