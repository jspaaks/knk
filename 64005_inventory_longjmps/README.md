# 64005_inventory_longjmps

## Build and install

```console
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```console
./dist/bin/inventory
```

Should output something like:

```console
```

