# 43400_inventory

## Build and install

```shell
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```shell
./dist/bin/inventory
```

Should output something like:

```text
```

