# 62501_quadratic_formula

## Build and install

```shell
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```shell
./dist/bin/quadratic
```

Should output something like:

```text
```

