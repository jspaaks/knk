# 58715_justify

## Build and install

```shell
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```shell
./dist/bin/justify
```

Should output something like:

```text
```

