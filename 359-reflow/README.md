
```
buffer = NCHARS_MAX + '*' + '\0'

while not end of file
clear buffer
while there's room in the buffer,
   read char from stdin
   if char is a whitespace, read next char
   if char is not whitespace, read into a buffer of length


while
   read char from stdin
   if char is some kind of whitespace, continue with next char
   # char is non-whitespace
   wordsend

```