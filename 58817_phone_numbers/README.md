# 58817_phone_numbers

## Build and install

```shell
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
```

## Run

```shell
./dist/bin/harmonize
```

Should output something like:

```text
```

